global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Can be extended to scrape other services on the monitoring-network
  # e.g., node-exporter, cadvisor, or application metrics
